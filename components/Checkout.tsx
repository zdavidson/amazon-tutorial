import { Box, Typography } from "@mui/material";
import React from "react";

const Checkout = () => {
  return (
    <Box>
      <Typography variant="h1">Checkout</Typography>
      <Box>
        <Typography variant="h2">1 Shipping Address</Typography>
      </Box>
    </Box>
  );
};

export default Checkout;
